DATABASE_URL=postgres://pughub:pughub_password@db:5432/pughub
STEAM_REALM=https://pughub.example.com
STEAM_RETURN_URL=https://pughub.example.com/api/auth/steam
SESSION_SECRET=change-me-to-a-long-random-string
STEAM_API_KEY=your-steam-api-key

# Optional overrides for the bundled postgres container
POSTGRES_USER=pughub
POSTGRES_PASSWORD=pughub_password
POSTGRES_DB=pughub

# Default server (auto-created if none exists)
DEFAULT_SERVER_NAME=Local Test Server
DEFAULT_SERVER_ADDRESS=127.0.0.1:27015
DEFAULT_SERVER_RCON_ADDRESS=127.0.0.1:27015
DEFAULT_SERVER_RCON_PASSWORD=change-me-rcon
DEFAULT_SERVER_PASSWORD=secret-password-here

# Restrict who can start scrims (comma-separated SteamIDs, leave empty for any creator)
SCRIM_START_STEAM_IDS=
